Learning Dynamic libraries.
```
Dynamic linking is a technique used by operating systems to link executable files with shared libraries at runtime instead of at compile time. In Linux, dynamic linking is commonly used to reduce the size of executable files and to allow multiple programs to share the same code in memory.

```
